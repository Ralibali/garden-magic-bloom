# WeeklyEmailSettings

Adds the opt-in/out control for the weekly email "Din odlingsvecka".

The component updates `profiles.weekly_email_enabled` directly and invalidates the existing `profile` query.
